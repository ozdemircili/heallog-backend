<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="ROBOTS" content="NOODP" />
    <meta name="viewport" content="initial-scale=1" />
    <title><%= title %></title>
    <%= include_ember_stylesheet_tags :patients_frontend %>
    <%= csrf_meta_tags %>
    <script src='https://connect.humanapi.co/connect.js'></script>
    <script>
      window.humanApiClientId = "<%= ENV.fetch("HUMANAPI_CLIENT_ID") %>"
      window.currentPatientId = <%= current_patient.id %>;
      <% if current_patient.has_humanapi_identity? %>
        window.currentPatientToken = "<%= current_patient.humanapi_identity.public_token %>";
      <% end %>
    </script>
  </head>
  <body>

    <%= yield %>

    <%= include_ember_script_tags :patients_frontend %>
  </body>
</html>

